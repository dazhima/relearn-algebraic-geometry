---
id: hartshorne-ii7-projective-morphisms
type: module
title: "Hartshorne II.7 — Projective morphisms: ample sheaves, linear systems, Proj and blowing up"
status: stub
parent: _AG-prep-index.md
depends_on:
  - hartshorne-ii6-divisors  # ⟦NEW⟧ candidate — no dedicated brick yet; §7 cites Pic X, Weil/Cartier divisors, linear equivalence from §6. See Setup.
used_by:
  - student-surfaces-thesis
technique: []
---

# Hartshorne II.7 — Projective morphisms

Statement numbers below are Hartshorne's own (7.1–7.17), transcribed from `materials/hartshorne1977.pdf` pp. 148–166, 2026-07-06. **This is a stub**: statements only, quoted close to the source; no proofs, no worked examples, no "Why" lines, no synthesis. Reconstructing any entry's proof, or actually instantiating an example, is what upgrades it — pick one entry and do that next, rather than reading this as if it were already understood.

## Idea

§7 = one machine (invertible sheaf + generating global sections $\leftrightarrow$ morphism to $\mathbb P^n$, Thm 7.1) run in four settings: closed-immersion criteria, ample sheaves, linear systems, and the same construction relative to a base $X$ (Proj of a graded sheaf, $\mathbb P(\mathscr E)$, blowing up).

## Setup

Notation imported from §6 (itself not yet a written brick — cited, not re-derived): Weil divisor $D=\sum n_Y Y$; Cartier divisor = section of $\mathscr K^*/\mathscr O^*$; on nonsingular $X/k=\bar k$ these agree (6.11) and $\operatorname{Cl}(X)\cong\operatorname{Pic}(X)$ via $D\mapsto\mathscr L(D)$ (6.15/6.16); $D\sim D'$ means $D-D'=(f)$ principal.

## Content

### A. Morphisms to $\mathbb P^n$

**D1.** Data: invertible sheaf $\mathscr L$ on an $A$-scheme $X$, plus global sections $s_0,\dots,s_n\in\Gamma(X,\mathscr L)$ generating $\mathscr L$.

**C1 (Thm 7.1).** $(\mathscr L,s_0,\dots,s_n)$ generating $\leftrightarrow$ unique $A$-morphism $\varphi:X\to\mathbb P^n_A$ with $\mathscr L\cong\varphi^*\mathscr O(1)$, $s_i=\varphi^*(x_i)$.
   — [ ] proof postponed

**E1 (7.1.1).** $\operatorname{Aut}_k(\mathbb P^n_k)=\operatorname{PGL}(n{+}1,k)$.
   — [ ] not instantiated

**C2 (Prop 7.2).** $\varphi$ is a closed immersion $\iff$ each $X_i$ affine and each $A[y_0,\dots,y_n]\to\Gamma(X_i,\mathscr O_{X_i})$ surjective.
   — [ ] proof postponed

**C3 (Prop 7.3).** For $k=\bar k$, $X$ projective over $k$: $\varphi$ is a closed immersion $\iff$ $V=\operatorname{span}(s_i)$ separates points and separates tangent vectors.
   — [ ] proof postponed

### B. Ample invertible sheaves

**D2 (p.153).** $\mathscr L$ on noetherian $X$ is ample if $\forall$ coherent $\mathscr F$, $\exists n_0$ s.t. $\mathscr F\otimes\mathscr L^n$ generated by global sections for all $n\ge n_0$.

**E2 (7.4.2).** $X$ affine $\Rightarrow$ every invertible sheaf ample.
   — [ ] not instantiated

**C4 (Prop 7.5).** $\mathscr L$ ample $\iff$ $\mathscr L^m$ ample $\forall m>0$ $\iff$ $\mathscr L^m$ ample for some $m>0$.
   — [ ] proof postponed

**C5 (Thm 7.6).** $X$ finite type$/A$ noetherian: $\mathscr L$ ample $\iff$ $\mathscr L^m$ very ample over $\operatorname{Spec} A$ for some $m>0$.
   — [ ] proof postponed

**E3 (7.6.1–7.6.3).** On $\mathbb P^n_k$: $\mathscr O(l)$ ample $\iff$ very ample $\iff$ $l>0$. On the quadric $Q$ (type $(a,b)$): ample $\iff$ very ample $\iff$ $a,b>0$. On an elliptic curve, $\mathscr O(P_0)$ is ample but not very ample.
   — [ ] not instantiated

### C. Linear systems

(Convention: nonsingular projective varieties over $k=\bar k$.)

**D3.** For $s\in\Gamma(X,\mathscr L)\setminus\{0\}$: divisor of zeros $(s)_0$.

**C6 (Prop 7.7).** Fix $D_0$, $\mathscr L=\mathscr L(D_0)$: (a) each $(s)_0\sim D_0$, effective; (b) every effective $D\sim D_0$ arises as some $(s)_0$; (c) $(s)_0=(s')_0\iff s'=\lambda s,\ \lambda\in k^*$.
   — [ ] proof postponed

**D4.** Complete linear system $|D_0|$ := all effective divisors $\sim D_0$ $\leftrightarrow$ $(\Gamma(X,\mathscr L)\setminus\{0\})/k^*$.

**D5.** Linear system $\mathfrak d\subseteq|D_0|$ = linear subspace, corresponding to $V\subseteq\Gamma(X,\mathscr L)$; $\dim\mathfrak d=\dim V-1$.

**C7 (Lemma 7.8).** $P$ base point of $\mathfrak d$ $\iff$ $s_P\in\mathfrak m_P\mathscr L_P\ \forall s\in V$. $\mathfrak d$ base-point-free $\iff$ $\mathscr L$ generated by $V$.
   — [ ] proof postponed

**E4 (7.8.3, 7.8.5, 7.8.6).** Degree-$d$ divisors on $\mathbb P^n$: complete, $\dim=\binom{n+d}{n}-1$. Twisted cubics in $\mathbb P^3$: all $\operatorname{PGL}(4,k)$-equivalent. Rational quartics in $\mathbb P^3$: not all equivalent.
   — [ ] not instantiated

### D. Proj of a sheaf of graded algebras; $\mathbb P(\mathscr E)$; blowing up

**D6 (†).** $X$ noetherian, $\mathscr S=\bigoplus_{d\ge0}\mathscr S_d$ quasi-coherent graded $\mathscr O_X$-algebra, $\mathscr S_0=\mathscr O_X$, $\mathscr S_1$ coherent, locally generated by $\mathscr S_1$. Construction: glue $\operatorname{Proj}\mathscr S(U)$ over affine $U$, giving $\operatorname{Proj}\mathscr S\xrightarrow{\pi}X$ with glued $\mathscr O(1)$.

**E5 (7.8.7 / Caution 7.8.8).** $\mathscr S=\mathscr O_X[T_0,\dots,T_n]\Rightarrow\operatorname{Proj}\mathscr S=\mathbb P^n_X$. Caution: $\mathscr O(1)$ need not be relatively very ample on a general $\operatorname{Proj}\mathscr S$.
   — [ ] not instantiated

**C8 (Lemma 7.9).** Twisting $\mathscr S$ by invertible $\mathscr L$ ($\mathscr S'_d=\mathscr S_d\otimes\mathscr L^d$) leaves $\operatorname{Proj}$ unchanged but $\mathscr O_{P'}(1)\cong\varphi^*\mathscr O_P(1)\otimes\pi'^*\mathscr L$.
   — [ ] proof postponed

**C9 (Prop 7.10).** (a) $\pi:\operatorname{Proj}\mathscr S\to X$ always proper. (b) $X$ has ample $\mathscr L$ $\Rightarrow$ $\pi$ projective, $\mathscr O(1)\otimes\pi^*\mathscr L^n$ very ample for suitable $n$.
   — [ ] proof postponed

**D7.** $\mathscr E$ locally free: $\mathbb P(\mathscr E):=\operatorname{Proj} S(\mathscr E)$.

**C10 (Prop 7.11).** (a) $S(\mathscr E)\cong\bigoplus_{l\in\mathbb Z}\pi_*\mathscr O(l)$. (b) natural surjection $\pi^*\mathscr E\twoheadrightarrow\mathscr O(1)$.
   — [ ] proof postponed

**C11 (Prop 7.12).** Morphisms $Y\to\mathbb P(\mathscr E)$ over $X$ $\leftrightarrow$ pairs $(\mathscr L$ on $Y$, surjection $g^*\mathscr E\twoheadrightarrow\mathscr L)$.
   — [ ] proof postponed

**D8.** Blow-up: $\mathscr I\subseteq\mathscr O_X$ coherent ideal, $\mathscr S=\bigoplus_{d\ge0}\mathscr I^d$ ($\mathscr I^0:=\mathscr O_X$ — the Rees algebra). $\widetilde X:=\operatorname{Proj}\mathscr S\xrightarrow{\pi}X$.

**E6 (7.12.1).** Blow-up of $\mathbb A^n_k$ at the origin recovers Ch. I §4's construction.
   — [ ] not instantiated

**C12 (Prop 7.13).** (a) $\pi^{-1}\mathscr I\cdot\mathscr O_{\widetilde X}$ invertible, $=\mathscr O_{\widetilde X}(1)$. (b) $\pi$ iso away from $V(\mathscr I)$.
   — [ ] proof postponed

**C13 (Prop 7.14, universal property).** $f:Z\to X$ with $f^{-1}\mathscr I\cdot\mathscr O_Z$ invertible $\Rightarrow$ $f$ factors uniquely through $\pi$.
   — [ ] proof postponed

**C14 (Cor 7.15).** $f:Y\to X$, $\mathscr J=f^{-1}\mathscr I\cdot\mathscr O_Y$; blow-up of $Y$ along $\mathscr J$ maps canonically to $\widetilde X$; closed immersion if $f$ is. Defines strict transform.
   — [ ] proof postponed

**C15 (Prop 7.16).** $X$ variety, $\mathscr I\neq0$: $\widetilde X$ variety; $\pi$ birational, proper, surjective; $X$ quasi-proj./proj. $\Rightarrow$ $\widetilde X$ too, $\pi$ projective.
   — [ ] proof postponed

**C16 (Thm 7.17).** Any birational projective morphism $f:Z\to X$ (varieties, $X$ quasi-projective$/k$) is a blow-up of $X$ along some coherent ideal sheaf.
   — [ ] proof postponed (Hartshorne: "somewhat difficult")

## Postponed

Everything above. Pick one entry to actually decompose next.
